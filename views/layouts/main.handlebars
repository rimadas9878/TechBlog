<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <link rel="stylesheet" href="/public/css/style.css" />
</head>

<body>
    <header>
        <h1>Welcome to the world of Tech Blogging</h1>
    </header>
    <nav>
        <a href="/">Home</a>
        <a href="/dashboard">Dashboard</a>
        {{#if logged_in}}
        {{!-- Using the "if" built-in helper such that if the user is logged in, show the logout button --}}
        <a href="/">Logout</a>
        {{else}}
        <a href="/">Login</a>
    </nav>
    <main>
        <div>
            {{!-- we don't want Handlebars to escape a value, hence using the "triple-stash" --}}
            {{{body}}}
        </div>
    </main>
    <!-- Render script for logged in users only -->
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>

</html>